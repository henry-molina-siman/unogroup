<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Sin parent interno (ensambles-parent eliminado, doc Implementación
       §1.1) — este repositorio es un proyecto Maven standalone, de un solo
       módulo. Vive en su propio repositorio, independiente de
       orquestador-app: sin pom.xml padre ni módulo común entre ellos. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.siman.ensambles</groupId>
  <artifactId>unogroup-app</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <properties>
    <java.version>21</java.version>
    <!-- Debe mantenerse igual a la de orquestador-app/pom.xml — ya no hay
         un pom padre común que lo imponga automáticamente (doc Implementación §6.2). -->
    <spring-cloud.version>2025.1.2</spring-cloud.version>
    <mapstruct.version>1.6.3</mapstruct.version>
    <!-- Solo para pruebas — no forman parte del contrato ni del runtime. -->
    <wiremock.version>3.9.2</wiremock.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct</artifactId>
        <version>${mapstruct.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mapstruct</groupId>
        <artifactId>mapstruct-processor</artifactId>
        <version>${mapstruct.version}</version>
      </dependency>
      <dependency>
        <groupId>org.wiremock</groupId>
        <artifactId>wiremock-standalone</artifactId>
        <version>${wiremock.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Sin dependencia a ensambles-common (eliminado, doc Implementación
         §1.1) — los enums de dominio que necesita se definen localmente. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <!-- Expone /actuator/health/readiness y /actuator/health/liveness —
         referenciados por k8s/deployment.yaml pero no listados en el
         pom.xml documentado (§6.3); adición mínima necesaria. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Cliente hacia Solution One y hacia el callback de orquestador-app -->
    <dependency>
      <groupId>org.springframework.cloud</groupId>
      <artifactId>spring-cloud-starter-openfeign</artifactId>
    </dependency>
    <!-- spring-cloud-starter-openfeign no trae feign-jackson por defecto
         (usa los HttpMessageConverters de Spring) — lo necesitamos
         explícitamente como fallback dentro de SolutionOneBinaryEncoder. -->
    <dependency>
      <groupId>io.github.openfeign</groupId>
      <artifactId>feign-jackson</artifactId>
    </dependency>
    <!-- Spring Boot 4.x usa internamente un ObjectMapper de "Jackson 3"
         (tools.jackson), incompatible con com.fasterxml.jackson.databind —
         no hay bean Spring del tipo Jackson 2 para autowire. Como
         SolutionOneCreatePayload/UpdatePayload usan anotaciones Jackson 2
         (mismo Jackson que feign-jackson, arriba), SolicitudProcesamientoService
         construye su propio ObjectMapper Jackson 2 en vez de depender de la
         autoconfiguración de Spring — jackson-datatype-jsr310 para serializar
         los campos Instant. -->
    <dependency>
      <groupId>com.fasterxml.jackson.datatype</groupId>
      <artifactId>jackson-datatype-jsr310</artifactId>
      <version>2.21.4</version>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.mapstruct</groupId>
      <artifactId>mapstruct</artifactId>
    </dependency>
    <dependency>
      <groupId>org.mapstruct</groupId>
      <artifactId>mapstruct-processor</artifactId>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- spring-boot-starter-parent ya liga "repackage" al lifecycle
           automáticamente al declarar este plugin (a diferencia de
           ensambles-parent, que no extendía de él y por eso antes
           necesitaba la <executions> explícita). -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </path>
            <path>
              <groupId>org.mapstruct</groupId>
              <artifactId>mapstruct-processor</artifactId>
              <version>${mapstruct.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
